{% extends 'base.html.twig' %}

{% block title %}Niveau{% endblock %}

{% block body %}
    <h1>Niveau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ niveau.niveau }}</td>
            </tr>
            <tr>
                <th>Parcours</th>
                <td>{{ niveau.parcours }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ niveau.Annee ? niveau.Annee|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveau_index') }}">back to list</a>

    <a href="{{ path('niveau_edit', {'id': niveau.id}) }}">edit</a>

    {{ include('niveau/_delete_form.html.twig') }}
{% endblock %}
