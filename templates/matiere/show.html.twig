{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>DesignMatiere</th>
                <td>{{ matiere.designMatiere }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ matiere.poids }}</td>
            </tr>
            <tr>
                <th>CreditMatiere</th>
                <td>{{ matiere.creditMatiere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('matiere_index') }}">back to list</a>

    <a href="{{ path('matiere_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
